# DeepSeek Harness bundle patch for jumpserver-dsh.
# Users override this row in their profile cordis.patch.yml (last write wins per id).
- insert:
    - id: jumpserver
      name: "@jumpserver-east/jumpserver-dsh"
